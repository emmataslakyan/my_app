package com.example.login;

import android.os.Bundle;
// Import BaseActivity if it's in a different package,
// or just use AppCompatActivity if BaseActivity isn't ready.

public class LanguagesActivity extends BaseActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_languages); // Make sure this XML exists

        // Your logic here
    }
}